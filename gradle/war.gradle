apply plugin: "war"

bootWar {
    mainClassName = "com.fcn.fanscoin.FansCoinApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
